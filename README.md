# Side Notes

一个贴在屏幕右侧的桌面便签工具。

## 功能

- 默认创建 3 个独立便签窗口
- 自动吸附到屏幕右侧，并按顺序纵向排列
- 点击左侧标签可缩进或展开便签
- 便签标题可编辑并自动保存
- 手动调整窗口高度后，下面的便签会自动下移对齐
- Todo 支持新增、编辑、完成、恢复和删除
- 长文本自动换行，内容在窗口内滚动
- 托盘菜单支持显示、隐藏、展开、缩进、开机启动和退出

## 开发

```bash
npm install
npm start
```

## 检查

```bash
npm test
```
